[project]
name = "kgraph"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
readme = "README.md"
dependencies = []

[dependency-groups]
dev = [
    "jupyter",
    "notebook",
    "matplotlib",
    "sphinx",
    "pylint",
    "black",
    "pytest-cov",
]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
